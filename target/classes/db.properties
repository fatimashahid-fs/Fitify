# ---------------------------------------------------------------
# Fitify - MySQL Database Configuration
# STEP 1: Create the database in MySQL:
#   CREATE DATABASE fitify CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
# STEP 2: Fill in your credentials below, then run:
#   mvn clean javafx:run
# ---------------------------------------------------------------
db.host     = localhost
db.port     = 3306
db.name     = fitify
db.user     = root
db.password = 1234